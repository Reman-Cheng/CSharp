using System;
using System.IO;
using System.Text;
using System.Threading;
using System.Collections;
using System.Threading.Tasks;
using System.Collections.Generic;//C#泛型命名空间
using System.Text.RegularExpressions;
using System.Security.Cryptography;
using System.Runtime.CompilerServices;
using System.Data.OleDb;
// using Microsoft.Office.Interop.Excel;
namespace MultithreadingApplication{
	public class MainThreadProgram{
		public static void Main(string[] args){
			// string path = "/Users/swqt/Desktop/SW QT 12.16-12.18加班申請匯總表.xls";
			// string strConn = "Provider=Microsoft.Jet.OLEDB.4.0;" +"Data Source="+ path +";"+"Extended Properties=Excel 8.0;"; 
			// OleDbConnection conn = new OleDbConnection(strConn); 
			// conn.Open();   
			// string strExcel = "";    
			// OleDbDataAdapter myCommand = null; 
			// DataSet ds = null; 
			// strExcel="select * from [sheet1$]"; 
			// myCommand = new OleDbDataAdapter(strExcel, strConn); 
			// ds = new DataSet(); 
			// myCommand.Fill(ds,"table1");  
			// Console.Write(ds);  

            // StreamReader sr = new StreamReader("/Users/swqt/Desktop/SW QT 12.16-12.18加班申請匯總表.xls");
            // Console.Write(sr.ReadToEnd());
            // sr.Close();
       }










	}
}